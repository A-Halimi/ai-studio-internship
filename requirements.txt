# ============================================================================
# AI Studio Internship - Python environment (laptops)
# ============================================================================
# GPU laptop (NVIDIA, Windows/Linux): install the CUDA build of PyTorch FIRST,
# then the rest of this file:
#
#   pip install torch==2.13.0 torchvision==0.27.1 --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements.txt
#
# CPU-only laptop: just run
#
#   pip install -r requirements.txt
#
# (the torch pins below resolve to the CPU wheels on PyPI; everything in the
# course auto-detects the device and falls back to CPU gracefully).
#
# Google Colab users: do NOT use this file - each notebook installs the few
# packages it needs in its first cell. Docker users: see docker/README section
# in the main README; the image already contains everything.
# ============================================================================

torch==2.13.0
torchvision==0.27.1

# Core scientific stack
# NOTE: numpy must stay <2.5 (umap-learn -> numba 0.66 requires it). Do not bump.
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
matplotlib==3.11.0
seaborn==0.13.2
plotly>=6.0,<7

# Modern AI
transformers==5.13.0
datasets==5.0.0

# Apps & interactivity
gradio==6.19.0
ipywidgets==8.1.8

# Reinforcement learning
gymnasium[classic-control,toy-text]==1.3.0
stable-baselines3==2.9.0

# Unsupervised extras
umap-learn==0.5.12

# Imaging / video
opencv-python-headless==4.13.0.92
imageio[ffmpeg]==2.37.3
pillow>=10.0

# Notebooks
jupyterlab==4.6.1

# Utilities (float - transitive deps of the pins above)
tqdm>=4.66
joblib>=1.3
